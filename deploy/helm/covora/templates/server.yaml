apiVersion: apps/v1
kind: Deployment
metadata:
  name: covora
  namespace: {{ .Release.Namespace }}
spec:
  replicas: {{ .Values.server.replicas }}
  selector:
    matchLabels:
      app: covora
  template:
    metadata:
      labels:
        app: covora
    spec:
      containers:
        - name: covora
          image: {{ .Values.image.registry }}/covora:{{ .Values.image.tag }}
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          ports:
            - containerPort: {{ .Values.server.port }}
          env:
            - name: PORT
              value: {{ .Values.server.port | quote }}
            - name: DATABASE_URL
              value: postgresql://{{ .Values.postgres.user }}:{{ .Values.postgres.password }}@postgres:5432/{{ .Values.postgres.db }}
            - name: OLLAMA_UI_BASE_URL
              value: 'http://ollama:11434'
            - name: OLLAMA_UI_MODEL
              value: {{ .Values.ollama.models.ui | quote }}
            - name: OLLAMA_CODE_BASE_URL
              value: 'http://ollama:11434'
            - name: OLLAMA_CODE_MODEL
              value: {{ .Values.ollama.models.code | quote }}
            - name: COVORA_AUTH_SECRET
              value: {{ .Values.auth.secret | quote }}
            - name: COVORA_ADMIN_EMAIL
              value: {{ .Values.auth.adminEmail | quote }}
            - name: COVORA_ADMIN_PASSWORD
              value: {{ .Values.auth.adminPassword | quote }}
          resources: {{- toYaml .Values.server.resources | nindent 12 }}
          readinessProbe:
            httpGet:
              path: /health
              port: {{ .Values.server.port }}
            initialDelaySeconds: 5
            periodSeconds: 10
---
apiVersion: v1
kind: Service
metadata:
  name: covora
  namespace: {{ .Release.Namespace }}
spec:
  selector:
    app: covora
  ports:
    - port: {{ .Values.server.port }}
      targetPort: {{ .Values.server.port }}
